---
deployment:
  tasks:
    # The generated cpanel-runtime branch contains the complete Node runtime.
    # server.js restores static assets and runs pending migrations on boot.
    # Keep this manifest intentionally minimal: cPanel only needs to signal
    # Passenger after checking out the new HEAD.
    - /bin/mkdir -p /home/jpmassag/repositories/ajmassagem-v3/tmp
    - /usr/bin/touch /home/jpmassag/repositories/ajmassagem-v3/tmp/restart.txt
